NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Optional in development; when omitted subscriber and sponsor data persist under .data/.
DATABASE_URL=
# Required only for GET /api/admin/export. Send as Authorization: Bearer <token>.
ADMIN_API_TOKEN=
